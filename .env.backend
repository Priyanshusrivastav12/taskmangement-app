# BACKEND ENVIRONMENT VARIABLES (.env)
# Copy this file to server/.env for local development

# Database Configuration
# MongoDB Atlas (Recommended for production)
MONGODB_URI=mongodb+srv://priyanshusrivastav548_db_user:Priyanshu%4012@cluster0.kxnemtr.mongodb.net/taskmanagement

# For local MongoDB (alternative)
# MONGODB_URI=mongodb://localhost:27017/taskmanagement

# JWT Authentication
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=mern-app-super-secret-jwt-key-2024
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Origins (update for production)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# ============================================
# DEPLOYMENT SPECIFIC ENVIRONMENT VARIABLES
# ============================================

# FOR RENDER DEPLOYMENT:
# Add these in Render Dashboard -> Environment Variables
#
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanagement
# JWT_SECRET=your-super-secure-random-jwt-secret-64-characters-long
# JWT_EXPIRES_IN=7d
# NODE_ENV=production
# PORT=10000
# CORS_ORIGINS=https://your-frontend-domain.vercel.app

# FOR RAILWAY DEPLOYMENT:
# Add these in Railway Dashboard -> Variables
#
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanagement
# JWT_SECRET=your-super-secure-random-jwt-secret-64-characters-long
# JWT_EXPIRES_IN=7d
# NODE_ENV=production
# PORT=${PORT} (Railway sets this automatically)
# CORS_ORIGINS=https://your-frontend-domain.vercel.app

# FOR HEROKU DEPLOYMENT:
# Add these using Heroku CLI or Dashboard
#
# heroku config:set MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/taskmanagement"
# heroku config:set JWT_SECRET="your-super-secure-random-jwt-secret-64-characters-long"
# heroku config:set JWT_EXPIRES_IN="7d"
# heroku config:set NODE_ENV="production"
# heroku config:set CORS_ORIGINS="https://your-frontend-domain.vercel.app"

# FOR VERCEL SERVERLESS FUNCTIONS:
# Add these in Vercel Dashboard -> Project Settings -> Environment Variables
#
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanagement
# JWT_SECRET=your-super-secure-random-jwt-secret-64-characters-long
# JWT_EXPIRES_IN=7d
# NODE_ENV=production

# Security Notes:
# - Never commit this file with real credentials to version control
# - Use strong, unique JWT secrets in production
# - Rotate JWT secrets periodically
# - Use MongoDB Atlas for production (includes security features)
# - Enable MongoDB Atlas Network Access restrictions
# - Use environment-specific database names (dev, staging, prod)

# MongoDB Atlas Setup:
# 1. Create account at https://www.mongodb.com/atlas
# 2. Create new cluster (free tier available)
# 3. Create database user with read/write permissions
# 4. Add your IP address to Network Access (or 0.0.0.0/0 for testing)
# 5. Get connection string and replace username/password
